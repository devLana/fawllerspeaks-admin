const useUnpublishPost = () => {
  const unpublishPost = (id: string) => {
    alert("Trying to unpublish a post with id of " + id);
  };

  return unpublishPost;
};

export default useUnpublishPost;
